# Create file structure

Takes a file with EEMs and absorbance data and creates subfolders to
store raw and processed data, plots, and output tables

## Usage

``` r
create_files(prjpath)
```

## Arguments

- prjpath:

  a string indicating the project file with the data
