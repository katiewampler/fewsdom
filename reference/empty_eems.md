# Check for EEMs that are empty

Check for EEMs that are empty

## Usage

``` r
empty_eems(eem, verbose = T)
```

## Arguments

- eem:

  object of class eem or eemlist

- verbose:

  if you want to print out which samples aren't empty

## Value

a vector of EEMs with empty EEMs tables (eem\$x)
